duration = 1000

pre =>
  main ->
    rotation-angle-x linear(90)
    y linear(0)
    opacity linear(0)

show =>
  main ->
    rotation-angle-x linear(0)
    y linear(0)
    opacity linear(255)

post =>
  main ->
    rotation-angle-x linear(0)
    y linear($height)
    opacity linear(0)
